## MVG distributions
If $X = [X_1, ..., X_d]$ is distributed as a MVG, then for every $i, j$ in ${1, ..., d}$, then $cov(X_i, X_j) = 0$  *iff* $X_i, X_j$ are **independent**

Generally (beyond MVG), weaker statement: *if* $X_i, X_j$ are independent, then $cov(X_i, X_j) = 0$
